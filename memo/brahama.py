"#a,'h=ref" # a string containing the HTML code for an anchor element
//                     + "href='#'"; // set href to '#', so that clicking it doesn't navigate anywhere (but still triggers
//                    + "href='#'"; // set href to '#', so that clicking on it doesn't navigate anywhere (stay
//                  "href="http://www.google.com/"'     // a string attribute of the 'a' tag, specifying the URL to which it should navigate
//                     "href": "http://www.google.com/"  # the href attribute of the anchor element, which should be a
//                     + "href='#' onclick='return false;'"  # add this to make it so that clicking on the link doesn
//                   + 'href="' + url    // add the URL to the href attribute of the anchor element
//                   + '"';               // close the opening tag
//     } else {
//         return '';
//     };
// });